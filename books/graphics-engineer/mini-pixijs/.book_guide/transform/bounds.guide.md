# 章节写作指导：Bounds 边界计算

## 1. 章节信息

- **章节标题**: Bounds 边界计算
- **文件名**: transform/bounds.md
- **所属部分**: 第七部分：Transform 与 Bounds
- **章节序号**: 41
- **预计阅读时间**: 22分钟
- **难度等级**: 高级

## 2. 学习目标

### 知识目标
- 理解 Bounds（边界）在渲染中的作用
- 掌握 PixiJS Bounds 类的实现
- 了解局部边界与世界边界的计算
- 理解容器边界的聚合策略

### 技能目标
- 能够获取和使用对象边界
- 能够理解边界更新机制
- 能够利用边界进行碰撞检测

## 3. 内容要点

### 核心概念（必须全部讲解）
- **Bounds 类**: 轴对齐包围盒（AABB）
- **localBounds**: 局部边界
- **getBounds()**: 获取世界边界
- **边界聚合**: 父容器包含所有子节点

### 关键知识点（必须全部覆盖）
- AABB 的表示：minX, minY, maxX, maxY
- Bounds 的核心方法：addFrame, addBounds, pad
- 局部边界的计算（基于对象本身）
- 世界边界的计算（变换后的边界）
- 容器边界的递归聚合
- 边界与 Rectangle 的转换
- 边界用途：裁剪、碰撞检测、布局

### 前置知识
- 第38-40章：Transform 系统
- 第33-37章：场景图基础

## 4. 写作要求

### 开篇方式
以"边界告诉我们对象占据了多大的空间"开篇，说明边界的基础作用。

### 结构组织
1. **引言**：边界的作用与重要性
2. **Bounds 类**：数据结构与方法
3. **局部边界**：对象自身的边界
4. **世界边界**：变换后的边界
5. **容器边界**：聚合子节点边界
6. **边界应用**：裁剪、碰撞、布局
7. **小结**：边界计算最佳实践

### 代码示例
- Bounds 创建与使用
- getBounds() 使用
- 边界聚合示例

### 图表需求
- **必须**：AABB 示意图
- **必须**：容器边界聚合示意图
- **可选**：变换对边界的影响

## 5. 技术细节

### 源码参考
- `packages/maths/src/misc/Bounds.ts`
- `packages/scene/src/container/bounds/`
- `packages/scene/src/container/Container.ts` getBounds 相关

### 实现要点
- 边界的初始化（无效状态）
- 边界的合并算法
- 变换后边界的计算（四角变换）
- 边界缓存策略

### 常见问题
- Q: 旋转后的边界为什么变大了？
  A: AABB 是轴对齐的，旋转后需要更大的矩形来包含
- Q: 如何获取精确的旋转边界？
  A: 使用 OBB 或多边形碰撞检测

## 6. 风格指导

### 语气语调
- 算法与实现结合
- 用图示辅助理解
- 解释设计权衡

### 类比方向
- 将 AABB 类比为"最小包装盒"—— 能装下对象的最小矩形
- 将边界聚合类比为"外包装"—— 包住所有内容

## 7. 章节检查清单

- [ ] 目标明确：本章输入/输出是否清晰
- [ ] 术语统一：是否给出标准引用与定义
- [ ] 最小实现：示例是否能运行且递进清晰
- [ ] 边界处理：异常/失败/限制是否覆盖
- [ ] 性能与权衡：优化思路与取舍是否明确
- [ ] 替代方案：对比与适用场景是否给出
- [ ] 图示与代码：是否一一对应
- [ ] 总结与练习：是否提供复盘与实操

## 8. 与其他章节的关系

### 前置章节
- 第38-40章：Transform 系统

### 后续章节
- 第42章：边界缓存策略
- 第43章：Culling 视锥裁剪
