# 章节写作指导：RenderLayer 渲染层

## 1. 章节信息

- **章节标题**: RenderLayer 渲染层
- **文件名**: culling/render-layer.md
- **所属部分**: 第八部分：Culling 与 Mask
- **章节序号**: 44
- **预计阅读时间**: 18分钟
- **难度等级**: 高级

## 2. 学习目标

### 知识目标
- 理解 RenderLayer 的概念与用途
- 掌握渲染层的创建与配置
- 了解层与渲染顺序的关系
- 理解层的隔离与性能优势

### 技能目标
- 能够使用 RenderLayer 组织场景
- 能够利用层实现复杂渲染效果
- 能够优化多层场景的性能

## 3. 内容要点

### 核心概念（必须全部讲解）
- **RenderLayer**: 渲染层，独立的渲染单元
- **层隔离**: 层内变换独立计算
- **层排序**: 层的渲染顺序
- **层缓存**: 层级渲染结果的缓存

### 关键知识点（必须全部覆盖）
- RenderLayer 的创建与使用
- 层与 Container 的关系
- 层的变换隔离
- 层内子节点的管理
- 层排序与 zIndex
- 层的性能优势
- 层的使用场景

### 前置知识
- 第33-37章：场景图
- 第43章：Culling

## 4. 写作要求

### 开篇方式
以"如何管理复杂场景中的渲染顺序？"开篇，引出 RenderLayer 的应用场景。

### 结构组织
1. **引言**：渲染层的需求
2. **RenderLayer 概念**：定义与特性
3. **创建与使用**：代码示例
4. **层隔离**：变换独立性
5. **层排序**：控制渲染顺序
6. **性能优势**：减少变换计算
7. **小结**：RenderLayer 使用指南

### 代码示例
- RenderLayer 创建
- 多层场景组织
- 层排序控制

### 图表需求
- **必须**：RenderLayer 结构图
- **可选**：层隔离效果示意

## 5. 技术细节

### 源码参考
- `packages/scene/src/container/RenderGroup.ts`
- `packages/scene/src/container/RenderLayer.ts`

### 实现要点
- RenderLayer 与 RenderGroup 的关系
- 层边界的计算
- 层的渲染指令生成
- 层缓存的实现

### 常见问题
- Q: RenderLayer 和普通 Container 有什么区别？
  A: RenderLayer 创建独立的渲染上下文，变换计算隔离
- Q: 层太多会影响性能吗？
  A: 每层有一定开销，但可以减少子节点变换计算

## 6. 风格指导

### 语气语调
- 实用导向
- 提供具体使用场景
- 解释设计决策

### 类比方向
- 将 RenderLayer 类比为"图层"—— 如 Photoshop 图层
- 将层隔离类比为"独立坐标系"—— 互不影响

## 7. 章节检查清单

- [ ] 目标明确：本章输入/输出是否清晰
- [ ] 术语统一：是否给出标准引用与定义
- [ ] 最小实现：示例是否能运行且递进清晰
- [ ] 边界处理：异常/失败/限制是否覆盖
- [ ] 性能与权衡：优化思路与取舍是否明确
- [ ] 替代方案：对比与适用场景是否给出
- [ ] 图示与代码：是否一一对应
- [ ] 总结与练习：是否提供复盘与实操

## 8. 与其他章节的关系

### 前置章节
- 第43章：Culling

### 后续章节
- 第45-47章：Mask 系统
