# 章节写作指导：GPUTexture 纹理管理

## 1. 章节信息

- **章节标题**: GPUTexture 纹理管理
- **文件名**: webgpu/gpu-texture.md
- **所属部分**: 第四部分：WebGPU 渲染器
- **章节序号**: 23
- **预计阅读时间**: 20分钟
- **难度等级**: 高级

## 2. 学习目标

### 知识目标
- 理解 WebGPU GPUTexture 的创建与配置
- 掌握纹理视图（GPUTextureView）概念
- 了解纹理格式与用途的关系
- 理解外部纹理（External Texture）

### 技能目标
- 能够创建各类 GPUTexture
- 能够上传图像数据到纹理
- 能够配置纹理视图

## 3. 内容要点

### 核心概念（必须全部讲解）
- **GPUTexture**: WebGPU 纹理对象
- **GPUTextureView**: 纹理视图，采样或渲染附件
- **GPUTextureFormat**: 纹理格式
- **GPUSampler**: 采样器对象
- **copyExternalImageToTexture**: 图像上传

### 关键知识点（必须全部覆盖）
- 纹理创建参数详解
- 纹理格式与兼容性
- 纹理用途标志（TEXTURE_BINDING、RENDER_ATTACHMENT）
- 纹理视图的创建与用途
- 采样器的独立性
- 与 WebGL 纹理的对比

### 前置知识
- 第16章：WebGL 纹理对比
- 第21章：GPU Device

## 4. 写作要求

### 开篇方式
以"WebGPU 将纹理和采样器分离"开篇，说明这一设计的意义。

### 结构组织
1. **引言**：WebGPU 纹理设计理念
2. **纹理创建**：参数与选项
3. **纹理格式**：常用格式介绍
4. **纹理视图**：用途与创建
5. **数据上传**：图像复制方法
6. **采样器**：独立配置
7. **小结**：纹理使用最佳实践

### 代码示例
- 纹理创建代码
- 图像上传代码
- 纹理视图创建
- 采样器配置

### 图表需求
- **必须**：Texture → View → Sampler 关系图
- **可选**：常用格式对比表

## 5. 技术细节

### 源码参考
- `packages/webgpu/src/texture/GpuTextureSystem.ts`
- `packages/webgpu/src/texture/GpuSamplerSystem.ts`

### 实现要点
- 纹理格式的能力检测
- Mipmap 的自动生成（需要额外 pass）
- 与 TextureSource 的抽象统一
- 纹理销毁

### 常见问题
- Q: 为什么采样器是独立对象？
  A: 可以复用采样器，减少对象创建
- Q: 如何生成 Mipmap？
  A: WebGPU 不自动生成，需要手动 pass 或使用 compute shader

## 6. 风格指导

### 语气语调
- 对比 WebGL 帮助理解
- 解释设计差异的原因
- 提供实际应用建议

### 类比方向
- 将 TextureView 类比为"窗口"—— 看纹理数据的方式
- 将 Sampler 类比为"放大镜设置"—— 如何读取像素

## 7. 章节检查清单

- [ ] 目标明确：本章输入/输出是否清晰
- [ ] 术语统一：是否给出标准引用与定义
- [ ] 最小实现：示例是否能运行且递进清晰
- [ ] 边界处理：异常/失败/限制是否覆盖
- [ ] 性能与权衡：优化思路与取舍是否明确
- [ ] 替代方案：对比与适用场景是否给出
- [ ] 图示与代码：是否一一对应
- [ ] 总结与练习：是否提供复盘与实操

## 8. 与其他章节的关系

### 前置章节
- 第16章：WebGL 纹理
- 第21章：GPU Device

### 后续章节
- 第48-53章：纹理系统详解
